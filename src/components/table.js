import React,{useState, useEffect} from "react";


function tableForm (){
  return(
  const [tableForm, settableform]

    useEffect (()=>{
      fetch ('http://localhost:3002/')
      .then(response => response.json())
      .then(data => setTransaction)
    })





    
  );

}

export default tableForm;